import React from "react";
import PlanGeneratorStoryboard from "./PlanGeneratorStoryboard";

const PlanGeneratorWrapper: React.FC = () => {
  return (
    <div className="bg-background min-h-screen">
      <PlanGeneratorStoryboard />
    </div>
  );
};

export default PlanGeneratorWrapper;
